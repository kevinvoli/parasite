import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { {{entityName}}Service } from './{{entityFile}}.service';
import { {{entityName}}Controller } from './{{entityFile}}.controller';
import { {{entityName}} } from './entities/{{entityFile}}.entity';
{{#if hasRelations}}
  {{#each relations}}
import { {{Capitalise this}} } from '../{{this}}/entities/{{this}}.entity';
  {{/each}}
{{/if}}

@Module({
    imports:[
        TypeOrmModule.forFeature([
        {{entityName}},
        {{#if hasRelations}}
          {{#each relations}}
            {{Capitalise this}} ,
          {{/each}}
        {{/if}}
         ]),
      ],
  controllers: [{{entityName}}Controller],
  providers: [{{entityName}}Service]
})
export class {{entityName}}Module {}
